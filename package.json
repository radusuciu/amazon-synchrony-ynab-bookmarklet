{
  "name": "amazon-synchrony-ynab-bookmarklet",
  "version": "0.1.2",
  "description": "Bookmarklet that helps you import transactions from the Amazon Synchrony Store Card activity page to a specific YNAB budget.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "vite build",
    "build:tsc": "tsc",
    "build:loader": "node scripts/update-loader.js",
    "build:all": "npm run build && npm run build:loader",
    "update-readme": "node scripts/update-loader.js --update-readme",
    "version:patch": "npm version patch",
    "version:minor": "npm version minor",
    "version:major": "npm version major",
    "preversion": "npm run build:all && npm run update-readme",
    "release": "npm run build:all && git push && git push --tags"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@tsconfig/node22": "^22.0.0",
    "@types/node": "^20.14.12",
    "@types/web": "^0.0.151",
    "terser": "^5.43.1",
    "typescript": "^5.5.4",
    "vite": "^5.3.5"
  }
}
